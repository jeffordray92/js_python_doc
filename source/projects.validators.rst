projects.validators module
==========================

The **projects.validators.py** module contains handy input validation functions to
ensure correct user input, as to avoid conflicts and errors corresponding to the project models.

.. automodule:: projects.validators
   :members: